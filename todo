team scores (through base report)
auto team creation
save hike configuration
use dictionary output for named results, e.g. Base.active

graphs:
estimated current team locations
area of possible team location
